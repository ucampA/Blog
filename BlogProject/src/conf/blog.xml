<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "dtd/mybatis-3-mapper.dtd">

<mapper namespace="Blog">

	<select id="selectAllPosts" resultType="blog">
		SELECT * FROM blog
		ORDER BY no
	</select>
	
	<select id="selectPostByNo" parameterType="int" resultType="blog">
		SELECT * FROM blog 
		WHERE no = #{no}
	</select>
	
	<insert id="insertPost" parameterType="blog">
		INSERT INTO blog
		VALUES (seq_blog_no.nextval, #{title}, #{content}, sysdate, 0, userid)
	</insert>
	
	<update id="updateReadCount">
		UPDATE blog
		SET readcount = readcount + 1
		WHERE no = #{no}
	</update>
	
	<update id="updatePost" parameterType="blog">
		UPDATE blog
		SET title = #{title}, content = #{content}
		WHERE no = #{no}
	</update>
	
	<delete id="deletePostByNo" parameterType="int">
		DELETE FROM blog 
		WHERE no = #{no}
	</delete>
	
	<delete id="deleteAllPosts">
		DELETE FROM blog 
	</delete>

</mapper>
